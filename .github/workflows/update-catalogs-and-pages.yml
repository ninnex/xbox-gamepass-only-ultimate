name: Update catalogs and deploy Pages

on:
  workflow_dispatch:
  schedule:
    - cron: "30 4 * * *"
      timezone: "America/New_York"

concurrency:
  group: xbox-gamepass-catalog-update
  cancel-in-progress: false

jobs:
  generate:
    name: Generate and validate catalogs
    runs-on: ubuntu-latest
    timeout-minutes: 90
    permissions:
      contents: write

    steps:
      - name: Apply random scheduled delay
        if: github.event_name == 'schedule'
        shell: bash
        run: |
          set -euo pipefail
          delay=$(( $(od -An -N4 -tu4 /dev/urandom) % 3600 ))
          echo "Scheduled delay: ${delay} seconds"
          echo "Delay started at: $(TZ=America/New_York date --iso-8601=seconds)"
          sleep "$delay"
          echo "Generation starting at: $(TZ=America/New_York date --iso-8601=seconds)"

      - name: Check out latest main
        uses: actions/checkout@v7
        with:
          ref: main
          fetch-depth: 0

      - name: Set up JDK 25 and Maven cache
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "25"
          cache: maven

      - name: Run tests
        run: ./mvnw --batch-mode verify

      - name: Generate catalogs
        run: |
          rm -rf build/generated-data
          ./mvnw --batch-mode exec:java -Dexec.args="build/generated-data data"

      - name: Validate generated files and print counts
        shell: bash
        run: |
          set -euo pipefail

          source_files=(
            build/generated-data/ultimate.csv
            build/generated-data/premium.csv
            build/generated-data/essential.csv
            build/generated-data/ea-play.csv
            build/generated-data/ubisoft-plus.csv
          )
          processed_files=(
            build/generated-data/ultimate-no-premium.csv
            build/generated-data/ultimate-exclusive.csv
          )
          expected_files=("${source_files[@]}" "${processed_files[@]}")

          mapfile -t actual_files < <(find build/generated-data -maxdepth 1 -type f -name '*.csv' -print | LC_ALL=C sort)
          mapfile -t sorted_expected < <(printf '%s\n' "${expected_files[@]}" | LC_ALL=C sort)

          if [[ "$(printf '%s\n' "${actual_files[@]}")" != "$(printf '%s\n' "${sorted_expected[@]}")" ]]; then
            echo 'Expected exactly these CSV files:' >&2
            printf '  %s\n' "${sorted_expected[@]}" >&2
            echo 'Found:' >&2
            printf '  %s\n' "${actual_files[@]}" >&2
            exit 1
          fi

          classified_source_files=(
            build/generated-data/ultimate.csv
            build/generated-data/premium.csv
            build/generated-data/essential.csv
          )
          unclassified_source_files=(
            build/generated-data/ea-play.csv
            build/generated-data/ubisoft-plus.csv
          )

          for file in "${classified_source_files[@]}"; do
            [[ -s "$file" ]] || { echo "$file is empty" >&2; exit 1; }
            header=$(head -n 1 "$file")
            [[ "$header" == 'name,productId,console,pc,category,storePath,newSinceDate' ]] || {
              echo "$file has an invalid header: $header" >&2
              exit 1
            }
          done

          for file in "${unclassified_source_files[@]}"; do
            [[ -s "$file" ]] || { echo "$file is empty" >&2; exit 1; }
            header=$(head -n 1 "$file")
            [[ "$header" == 'name,productId,console,pc,storePath,newSinceDate' ]] || {
              echo "$file has an invalid header: $header" >&2
              exit 1
            }
          done

          for file in "${processed_files[@]}"; do
            [[ -s "$file" ]] || { echo "$file is empty" >&2; exit 1; }
            header=$(head -n 1 "$file")
            [[ "$header" == 'name,productId,console,pc,category,storePath,newSinceDate' ]] || {
              echo "$file has an invalid header: $header" >&2
              exit 1
            }
          done

          for file in "${expected_files[@]}"; do
            if head -c 3 "$file" | cmp -s - <(printf '\xEF\xBB\xBF'); then
              echo "$file contains a forbidden UTF-8 BOM" >&2
              exit 1
            fi
            if LC_ALL=C grep -q $'\r' "$file"; then
              echo "$file contains a forbidden carriage return" >&2
              exit 1
            fi
          done

          info=build/generated-data/catalog-info.json
          [[ -s "$info" ]] || { echo "$info is empty" >&2; exit 1; }
          jq -e '
            keys == ["changesFound", "lastCheckedAt", "newGameDisplayDays", "xboxStoreBaseUrl"] and
            .xboxStoreBaseUrl == "https://www.xbox.com/en-US/games/store/" and
            (.newGameDisplayDays | type == "number") and
            .newGameDisplayDays > 0 and
            (.lastCheckedAt | type == "string") and
            (.changesFound | type == "boolean")
          ' "$info" >/dev/null

          echo 'Generated catalog counts:'
          for file in "${expected_files[@]}"; do
            rows=$(( $(wc -l < "$file") - 1 ))
            printf '  %s: %d games\n' "$file" "$rows"
          done

          echo 'Data change summary:'
          echo "changesFound: $(jq -r '.changesFound' "$info")"

      - name: Publish validated candidate set to the working tree
        shell: bash
        run: |
          set -euo pipefail
          files=(
            ultimate.csv
            premium.csv
            essential.csv
            ea-play.csv
            ubisoft-plus.csv
            ultimate-no-premium.csv
            ultimate-exclusive.csv
            catalog-info.json
          )
          for file in "${files[@]}"; do
            cp "build/generated-data/$file" "data/$file"
          done
          echo 'Data change summary:'
          git diff --stat -- data/ || true

      - name: Commit and push valid catalog changes
        id: publish_data
        shell: bash
        run: |
          set -euo pipefail

          files=(
            data/ultimate.csv
            data/premium.csv
            data/essential.csv
            data/ea-play.csv
            data/ubisoft-plus.csv
            data/ultimate-no-premium.csv
            data/ultimate-exclusive.csv
            data/catalog-info.json
          )

          if git diff --quiet -- "${files[@]}"; then
            echo 'No generated data changes detected; no commit will be created.'
            git fetch origin main
            git merge --ff-only origin/main
            echo 'changed=false' >> "$GITHUB_OUTPUT"
            exit 0
          fi

          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -- "${files[@]}"

          mapfile -t staged_files < <(git diff --cached --name-only | LC_ALL=C sort)
          [[ ${#staged_files[@]} -gt 0 ]] || {
            echo 'No catalog changes remained after staging.' >&2
            exit 1
          }

          for staged_file in "${staged_files[@]}"; do
            allowed=false
            for expected_file in "${files[@]}"; do
              if [[ "$staged_file" == "$expected_file" ]]; then
                allowed=true
                break
              fi
            done
            [[ "$allowed" == true ]] || {
              echo "Refusing to commit unexpected file: $staged_file" >&2
              exit 1
            }
          done

          git commit -m 'chore(data): update Xbox Game Pass catalogs'
          git fetch origin main
          git rebase origin/main
          git push origin HEAD:main
          git fetch origin main

          [[ "$(git rev-parse HEAD)" == "$(git rev-parse origin/main)" ]] || {
            echo 'Remote main does not match the generated commit.' >&2
            exit 1
          }

          echo 'changed=true' >> "$GITHUB_OUTPUT"

      - name: Prepare clean Pages artifact
        shell: bash
        run: |
          set -euo pipefail
          rm -rf build/pages
          mkdir -p build/pages/data
          cp index.html build/pages/index.html
          cp data/*.csv build/pages/data/
          cp data/catalog-info.json build/pages/data/

          test "$(find build/pages -type f | wc -l)" -eq 9
          test -f build/pages/index.html
          test -f build/pages/data/catalog-info.json
          test "$(find build/pages/data -maxdepth 1 -type f -name '*.csv' | wc -l)" -eq 7

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: build/pages

  deploy:
    name: Deploy GitHub Pages
    needs: generate
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
